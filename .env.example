# Google Generative AI (Gemini) Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# Google Cloud Text-to-Speech Service Account (base64 encoded)
# For local development, you can use: GOOGLE_APPLICATION_CREDENTIALS=./credentials.json
# For Vercel, encode credentials.json as base64 and set as GOOGLE_CREDENTIALS_B64
GOOGLE_CREDENTIALS_B64=your_base64_encoded_credentials_here

# Server Configuration
PORT=3000
NODE_ENV=development